/** Sticky app bar height (matches Tailwind h-14). */
export const APP_STICKY_HEADER_PX = 56;

/** Nauta logo height — three quarters of the sticky header. */
export const NAUTA_LOGO_HEIGHT_PX = Math.round(APP_STICKY_HEADER_PX * 0.75);
